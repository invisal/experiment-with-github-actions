name: Reusable workflow example

on:
  workflow_call:
    inputs:
      message:
        required: true
        type: string

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello ${{ inputs.message }}.
        shell: bash
